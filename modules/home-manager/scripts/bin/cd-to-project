#!/usr/bin/env bash

selected=$(find ~/Documents/repositories -mindepth 1 -maxdepth 2 -type d -not -iwholename '*.git*' | fzf)
if [ -n "$selected" ]; then
	zellij action new-tab --name "$(basename "$selected")" --cwd "$selected" || exit
fi
